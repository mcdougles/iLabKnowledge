### Customize your application-conf
The `application-conf` directory contains various different language configurations used by DBB. You have to modify the files accordingly.

It is required to modify the `datasets.properties` file for all applications but most applications will also need to make changes to other properties files in that folder.
